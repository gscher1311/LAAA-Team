services:
  - type: web
    name: land-residual-analysis
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm run web
    envVars:
      - key: NODE_ENV
        value: production
    plan: free
