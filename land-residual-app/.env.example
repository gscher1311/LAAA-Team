# Land Residual Analysis App - Environment Variables
# Copy this file to .env.local and fill in your values

# Main app password (required)
# Users must enter this password to access the application
LAAA_PASSWORD=change-this-to-your-secure-password

# JWT Secret for session tokens (required)
# Generate with: openssl rand -base64 32
JWT_SECRET=generate-a-random-secret-key

# Base URL for share links (required for production)
# Set to your production domain
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Database path (optional)
# Defaults to ./data/laaa.db if not set
# DATABASE_PATH=/path/to/persistent/storage/laaa.db
